-r requirements-prod.txt

# test runner + http client p/ TestClient
pytest==9.0.3
pytest-cov==7.1.0
pytest-asyncio==1.3.0
pytest-mock==3.15.1
httpx==0.28.1
# teste de contrato IC-02 (valida payloads CBS/IBS contra os JSON Schemas)
jsonschema==4.23.0

# lint / format / type / security (informativo no CI; usar via make lint/security)
ruff
black
bandit
pip-audit
semgrep
