// Estilo compartilhado do Tooltip do Recharts (usa tokens do tema).
export const RECHARTS_TOOLTIP_STYLE = {
  background: "hsl(var(--card))",
  border: "1px solid hsl(var(--border))",
  borderRadius: 8,
  fontSize: 12,
} as const;
