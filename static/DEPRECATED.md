# UI legada (deprecated)

A interface principal passou para `orgconc-react/`, servida em `/app/` após `npm run build`.

Este diretório (`static/`) permanece em `/ui/` apenas durante o período de transição.
